venv/
__pycache__/
*.pyc
# Regenerable eval run outputs — the numbers live in EVALUATION.md
scripts/eval_results/
.env
.DS_Store
data/raw/
*.pdf
AGENTS.md
CHECKLIST.md
# DrugBank academic data is license-restricted (CC BY-NC) — never commit it.
data/drugbank/*
!data/drugbank/README.md
# DDInter raw CSVs — open-access but not ours to redistribute; keep local.
data/ddinter/*
!data/ddinter/README.md
# The derived pair lookup (our drug names -> severity, cited in data/ddinter/README.md)
# IS committed so the deployed app has DDInter. To keep it local instead, re-add:
#   data/processed/ddinter_pairs.json
