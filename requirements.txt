# Runtime dependencies — what the deployed API needs.
# Build/extraction-only deps are in requirements-build.txt; test deps in requirements-dev.txt.

# API + UI
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
jinja2

# Data + LLM + RAG
pandas>=2.0
openai>=1.72
pinecone>=5.0
langgraph>=0.2
langchain-core>=0.3

# Config
python-dotenv>=1.0
